apiVersion: v1
kind: Service

metadata:
  name: ecommerce-web-service
  namespace: ecommerce

spec:
  selector:
    app: ecommerce-web

  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000

  type: NodePort